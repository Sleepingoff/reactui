import { Meta } from '@storybook/react';

import Panel from './Panel';

const meta: Meta = {
  title: 'Components/molecules/Panel',
  component: Panel,
  tags: ['autodocs'],
  args: {}
};

export default meta;
