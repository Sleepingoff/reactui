import { Meta } from '@storybook/react';

import Details from './Details';

const meta: Meta = {
  title: 'Components/organisms/Details',
  component: Details,
  tags: ['autodocs'],
  args: {}
};

export default meta;
